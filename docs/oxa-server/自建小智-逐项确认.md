# 自建小智配置 — 逐项确认

按顺序逐条确认，在 **你** 或 **我** 确认后，在对应条目前的 `[ ]` 里改成 `[x]`。

---

## ① 小智服务与智控台已启动

- **我这边已查**：NAS 上 4 个容器均为 Up（xiaozhi-esp32-server、db、redis、web），智控台端口 18002 已映射。
- **请你确认**：浏览器打开 **http://<你的NAS局域网IP>:18002** 能打开并登录。

- [x] **① 已确认**

---

## ② 参数管理：server.ota、server.websocket

- **请你操作**：智控台 → **参数字典 / 参数管理**，确认：
  - **server.ota** = `http://<你的NAS IP>:18002/xiaozhi/ota/`（按实际 IP 修改）
  - **server.websocket** = `ws://<你的NAS IP>:8000/xiaozhi/v1/`
- 若有「保存」则保存。

- [x] **② 已确认**

---

## ③ NAS 上 data/.config.yaml 的 manager-api.secret

- **请你确认**：NAS 上 `data/.config.yaml` 中 `manager-api.secret` 是否与智控台 **参数管理** 里 **server.secret** 的值一致（从智控台复制粘贴，勿将真实 secret 写入公开仓库）。

- [x] **③ 已确认**

---

## ④ 模型配置：大语言模型（LLM）+ API Key

- **请你操作**：智控台 → **模型配置** → **大语言模型**：
  - 是否已添加并启用一个 LLM（如智谱 glm-4-flash、豆包、Minimax 等）？
  - 该模型的 **API 密钥** 是否已填写且有效？（智谱：<https://open.bigmodel.cn/> 或 bigmodel.cn 创建密钥后粘贴）
- 若尚未配置：先去对应平台注册并创建 API Key，再在此处填写并保存。

- [x] **④ 已确认**

---

## ⑤ 模型配置：语音合成（TTS）

- **请你操作**：智控台 → **模型配置** → **语音合成（TTS）**：
  - 是否已添加并启用一个 TTS（如灵犀流式 Linkerai、火山引擎等）？
  - 若有 API Key / 应用 ID 等要求，是否已填写并保存？

- [x] **⑤ 已确认**

---

## ⑥ 智能体管理：智能体已选 LLM + TTS

- **请你操作**：智控台 → **智能体管理** → 打开你要用的那个智能体（如「爸爸最帅」）：
  - **大语言模型(LLM)** 是否已选为你在 ④ 里配置的模型？
  - **语音合成(TTS)** 是否已选为你在 ⑤ 里配置的 TTS？
- 保存配置。若提示「保存后需重启设备」，则执行：`docker restart xiaozhi-esp32-server`。

- [ ] **⑥ 已确认**

---

## ⑦ 设备管理：设备已用验证码绑定

- **说明**：Open-XiaoAI 首次连自建小智时，会得到验证码（日志或智控台设备管理里可见）。
- **请你确认**：智控台 → **设备管理**，是否已用该验证码添加/绑定了你的设备？若尚未绑定，先完成 ④⑤⑥，再改 Open-XiaoAI 的 config 连自建小智，触发一次对话后取验证码并在此绑定。

- [ ] **⑦ 已确认**

---

## ⑧ Open-XiaoAI 的 config.py：只改小智服务器三行

- **约定**：只改 **OTA_URL**、**WEBSOCKET_URL**（及必要时 VERIFICATION_CODE），其余不改。
- **自建小智时** 改为（NAS IP 按你的改）：
  - `OTA_URL`: `http://192.168.1.100:18002/xiaozhi/ota/`
  - `WEBSOCKET_URL`: `ws://192.168.1.100:8000/xiaozhi/v1/`
- 若 Open-XiaoAI 与 xiaozhi 同机 Docker，也可用容器名（并让 Open-XiaoAI 加入 xiaozhi 网络）：
  - `OTA_URL`: `http://xiaozhi-esp32-server-web:8002/xiaozhi/ota/`
  - `WEBSOCKET_URL`: `ws://xiaozhi-esp32-server:8000/xiaozhi/v1/`

- [ ] **⑧ 已确认**

---

## ⑨ 重启使配置生效

- **请你或我执行**：
  - `docker restart xiaozhi-esp32-server`
  - `docker restart open-xiaoai-xiaozhi`
- 等待约 30 秒后再试唤醒与对话。

- [ ] **⑨ 已确认**

---

从 **①** 开始，你确认完一项就回复「① 确认」或「① 过了」，我们再做下一项；若某一步不会操作或报错，直接说第几步、现象或截图，我按步帮你排查。
